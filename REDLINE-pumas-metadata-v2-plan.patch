diff --git a/docs/plans/PUMAS_LIBRARY_METADATA_V2_CONSUMER_MIGRATION.md b/docs/plans/PUMAS_LIBRARY_METADATA_V2_CONSUMER_MIGRATION.md
index 0000000..1111111 100644
--- a/docs/plans/PUMAS_LIBRARY_METADATA_V2_CONSUMER_MIGRATION.md
+++ b/docs/plans/PUMAS_LIBRARY_METADATA_V2_CONSUMER_MIGRATION.md
@@ -47,12 +47,13 @@
 11. model card and license artifact metadata (path/source/hash/status)
 
 Validation requirements:
-1. Known `model_type` enum value.
-2. Known `task_type_primary` value.
+1. `model_type` must be a member of canonical enum values, including `unknown`.
+2. `task_type_primary` must be a canonical task tag or `unknown`.
 3. Non-empty input/output modalities.
 4. Bounded confidence value.
 5. Custom code sources required when `requires_custom_code=true`.
 6. Referenced dependency profile must exist.
+7. If `model_type=unknown` or `task_type_primary=unknown`, then `metadata_needs_review=true`, `review_reason` is required, and `classification_confidence=0.0`.
 
 ## Classification Policy (Task Signature Driven)
 Classification is based on source task signature (`from -> to`), including multimodal signatures like `text+image->image`.
@@ -64,7 +65,7 @@
 
 Decision behavior:
 1. If signature mapping exists, set `task_type_primary` and `model_type` from mapping.
-2. If signature mapping does not exist, set `model_type=unknown`, `task_type_primary=unknown`, and `metadata_needs_review=true`.
+2. If signature mapping does not exist, set `model_type=unknown`, `task_type_primary=unknown`, `metadata_needs_review=true`, `review_reason=unknown-task-signature`, `classification_source=runtime-discovered-signature`, and `classification_confidence=0.0`.
 3. Unknown/ambiguous classification never blocks download/import.
 
 ## SQLite Mutable Mapping Registry
@@ -75,14 +76,17 @@
 ```sql
 CREATE TABLE IF NOT EXISTS task_signature_mappings (
   id INTEGER PRIMARY KEY,
-  signature_key TEXT NOT NULL UNIQUE,
-  input_modalities_json TEXT NOT NULL,
-  output_modalities_json TEXT NOT NULL,
-  task_type_primary TEXT NOT NULL,
-  model_type TEXT NOT NULL,
+  signature_key TEXT NOT NULL,
+  input_modalities_json TEXT NOT NULL DEFAULT '[]',
+  output_modalities_json TEXT NOT NULL DEFAULT '[]',
+  task_type_primary TEXT NOT NULL DEFAULT 'unknown',
+  model_type TEXT NOT NULL DEFAULT 'unknown',
   priority INTEGER NOT NULL DEFAULT 100,
   status TEXT NOT NULL DEFAULT 'active',
   source TEXT NOT NULL DEFAULT 'system',
+  discovered_count INTEGER NOT NULL DEFAULT 0,
+  first_seen_at TEXT,
+  last_seen_at TEXT,
   notes TEXT,
   created_at TEXT NOT NULL,
   updated_at TEXT NOT NULL
@@ -90,6 +94,10 @@
 
 CREATE INDEX IF NOT EXISTS idx_task_signature_mappings_lookup
   ON task_signature_mappings(status, signature_key, priority);
+
+CREATE UNIQUE INDEX IF NOT EXISTS idx_task_signature_mappings_active_unique
+  ON task_signature_mappings(signature_key)
+  WHERE status = 'active';
 ```
 
 Initial mapping table draft:
@@ -116,11 +124,12 @@
 
 Runtime mutability rules:
 1. Normalize source signature into `signature_key`.
-2. Lookup active mapping by signature and priority.
-3. If missing, insert pending row with `source='runtime-discovered'` and unknown mapping values.
+2. Lookup active mapping by signature and highest priority.
+3. If no active mapping exists, upsert a pending row with `source='runtime-discovered'`, `task_type_primary='unknown'`, `model_type='unknown'`, incremented `discovered_count`, and updated `last_seen_at`.
 4. Mark model metadata for review with reason `unknown-task-signature`.
 5. Continue import/download without blocking.
-6. Activate new mapping rows later via migration/admin tooling without code release.
+6. Activate mappings by explicit state transition (`pending` to `active`) via migration/admin tooling.
+7. Activation must be audited with actor, timestamp, and change reason.
 
 ## Canonical Library Reorganization
 Canonical path format remains:
@@ -142,17 +151,46 @@
 Dependency profiles:
 1. Use reusable model-level profiles rather than duplicating per model.
 2. Profile fields include environment kind, requirement sources, platform constraints, install policy, validation probes, notes.
+3. Profiles are immutable after publish; updates create new `profile_version`.
+
+Canonical dependency profile contract:
+1. `profile_id`
+2. `profile_version`
+3. `environment_kind` (`python-venv`, `binary`, `mixed`)
+4. `requirements_sources` (file refs, inline requirements, wheel URLs, git refs)
+5. `platform_constraints` (os, arch, cuda/rocm)
+6. `install_policy` (`on-demand`, `eager`, `manual-approval-required`)
+7. `validation_probes` (import checks, executable checks)
+8. `notes`
+9. `profile_hash` (content hash used for environment reuse and conflict detection)
+
+Canonical persistence tables (SQLite):
+1. `dependency_profiles(profile_id, profile_version, profile_hash, environment_kind, spec_json, created_at, PRIMARY KEY(profile_id, profile_version))`
+2. `model_dependency_bindings(model_id, profile_id, profile_version, attached_by, attached_at, FOREIGN KEY(profile_id, profile_version) REFERENCES dependency_profiles(profile_id, profile_version))`
 
 Model-level dependency APIs:
 1. `get_model_dependency_profile(model_id)`
 2. `check_model_dependencies(model_id, env_id, platform_context)`
 3. `install_model_dependencies(model_id, env_id, platform_context)`
+4. `list_models_needing_review(filter)`
+5. `submit_model_review(model_id, patch, reviewer)`
+
+Dependency API response contract:
+1. `state` enum is one of `ready | missing | installing | failed | unknown_profile | review_required`.
+2. Responses include `profile_id`, `profile_version`, `profile_hash`, `env_id`, and structured per-component status.
+3. Failures return deterministic error codes suitable for consumer UX and telemetry.
+
+Environment resolution rules:
+1. If not provided, `env_id` defaults to `{environment_kind}:{profile_id}:{profile_version}:{profile_hash}:{platform_key}`.
+2. Matching `env_id` plus `profile_hash` reuses an existing environment.
+3. Same `env_id` with different `profile_hash` fails with `profile_conflict`.
 
 Operational rules:
 1. Public internet sources are allowed (including GitHub and Hugging Face).
 2. Dependency install attempts must log source URL/ref and result.
 3. Failures must return structured actionable errors.
 4. Offline mode fails fast if required dependencies are absent.
+5. Install operations must be idempotent and safe to retry.
 
 ## License Governance Policy
 1. Fetch and retain license artifact locally for each model during import or migration.
@@ -200,6 +238,7 @@
 1. Replace legacy metadata parsing with v2 fields.
 2. Replace legacy dependency calls with model-level APIs.
 3. Treat `metadata_needs_review=true` as non-ready for automatic routing unless explicitly overridden.
+4. Treat `unknown_profile` as non-ready and surface `create-or-attach-dependency-profile` remediation.
 
 Phase 2: Path/ID Refactor
 1. Remove assumptions about stable absolute model paths.
@@ -227,6 +266,7 @@
 3. Do not silently fallback to deprecated classification behavior.
 4. Use model dependency APIs before inference where dependency profiles exist.
 5. Never call removed legacy endpoints or parse removed legacy fields.
+6. Never auto-route models with `model_type=unknown` or `task_type_primary=unknown` unless policy explicitly allows it.
 
 ## Rollback Strategy
 Because legacy methods are removed, rollback is snapshot/artifact based:
@@ -248,7 +288,8 @@
 3. Directory reorganization report is generated and validated.
 4. Unknown signatures are captured in SQLite pending mappings without blocking operations.
 5. License artifacts are retained; unresolved license is represented as `license_unknown`.
-6. Binding contract tests and core tests pass in cutover build.
+6. Dependency profile resolution enforces `profile_id/profile_version/profile_hash` consistency.
+7. Binding contract tests and core tests pass in cutover build.
 
 ## Current Reference Docs
 1. `/media/jeremy/OrangeCream/Linux Software/Pantograph/PROPOSAL-pumas-library-metadata-dependency-v2.md`
