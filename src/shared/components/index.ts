/**
 * Shared Components
 *
 * Reusable UI components that can be used across features.
 */

// Re-export from hotload-sandbox
export {
  ErrorPlaceholder,
  SafeComponent,
  ComponentContainer,
} from '../../lib/hotload-sandbox';
