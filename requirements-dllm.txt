# dLLM / TraDo model support
# The SDAR architecture code ships inside the HuggingFace model repo
# (modeling_sdar.py, configuration_sdar.py) and is loaded automatically
# via trust_remote_code=True — no extra pip package needed.
#
# This extras file installs huggingface_hub for downloading complete
# model repos (weights + config + tokenizer + custom code).
huggingface_hub
# flash_attn must be installed from a pre-built wheel matching your
# torch/CUDA/Python versions. Building from source compiles CUDA kernels
# and will freeze most machines. Pre-built wheels:
#   https://github.com/mjun0812/flash-attention-prebuild-wheels/releases
# Current: torch 2.10, CUDA 12.8, Python 3.12, Linux x86_64
https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.7.16/flash_attn-2.8.3%2Bcu128torch2.10-cp312-cp312-linux_x86_64.whl
