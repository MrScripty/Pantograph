[package]
name = "pantograph-runtime-identity"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared backend identity and alias normalization for Pantograph runtime surfaces"

[dependencies]
